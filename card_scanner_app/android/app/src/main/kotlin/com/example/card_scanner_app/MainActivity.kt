package com.example.card_scanner_app

import io.flutter.embedding.android.FlutterActivity

class MainActivity : FlutterActivity() {
    // No need to override configureFlutterEngine if you don't have custom plugins
}
